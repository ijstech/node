{
    "name": "@ijstech/plugin",
    "version": "0.1.0",
    "main": "lib/index.js",
    "pluginTypes": "plugin/index.d.ts",
    "types": "types/index.d.ts",    
    "scripts": {
        "build": "rm -rf lib && rm -rf types && rm -f tsconfig.tsbuildinfo && tsc && cp -rf src/lib lib",
        "test": "mocha --exit --require ts-node/register test/*.spec.ts"
    }
}