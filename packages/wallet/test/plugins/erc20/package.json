{
    "name": "erc20",
    "main": "lib/index/js",
    "types": "types/index.d.ts",
    "dependencies": {
        "@ijstech/eth-contract": "^0.1.3"
    },
    "devDependencies": {
        "@ijstech/compiler": "^0.1.15"
    },
    "scripts": {
        "build": "rm -rf lib && rm -rf types && tsc && npx @ijstech/compiler",
        "build:contract": "sol2ts solconfig.json"
    }
}
