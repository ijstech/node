{
    "name": "@ijstech/storage",
    "description": "Storage Plugin",
    "version": "0.1.0",
    "main": "./lib/index.js",
    "types": "./types/index.d.ts",
    "devDependencies": {
        "@types/node": "18.11.9",
        "web3.storage": "4.5.4"
    },
    "scripts": {
        "build": "rm -rf lib && rm -rf types && rm -f tsconfig.tsbuildinfo && tsc",
        "test": "mocha --exit --require ts-node/register test/*.spec.ts"
    }
}
