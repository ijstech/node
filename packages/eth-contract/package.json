{
    "name": "@ijstech/eth-contract",
    "version": "0.1.4",
    "bin": {
        "sol2ts": "tools/bin/compile.js"
    },
    "dependencies": {
        "bignumber.js": "9.0.1"        
    },
    "devDependencies": {
        "@types/node": "14.17",
        "solc": "0.8.12",
        "typescript": "4.2.4"
    },
    "main": "lib/index.js",
    "types": "types/index.d.ts",
    "scripts": {
        "build": "rm -rf lib && rm -rf dist && rm -rf types && tsc && tsc --p tsconfig.lib.json",
        "build:tools": "rm -rf tools/bin && cd tools && rm -rf bin && tsc && cd ..",
        "test": "mocha --exit --require ts-node/register test/*.spec.ts"
    }
}