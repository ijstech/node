{
    "name": "@pack/demo",
    "version": "0.1.0",
    "main": "./lib/index.js",
    "types": "./types",
    "dependencies": {
        "@ijstech/pdm": "0.1.0"
    },
    "scripts": {        
        "build": "rm -rf lib && rm -rf types && tsc --p tsconfig.types.json && tsc --p tsconfig.lib.json "
    }
}