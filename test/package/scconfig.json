{    
    "routes": [{
            "baseUrl": "/hello",
            "scriptPath": "router.ts"
        }
    ],
    "jobs": [
        {
            "cron": "*/4 * * * * *",
            "scriptPath": "worker.ts",            
            "params": {
                "param1": "param_value1"
            }
        }
    ]
}